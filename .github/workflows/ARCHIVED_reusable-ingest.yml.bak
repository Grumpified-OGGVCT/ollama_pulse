# ARCHIVED: This workflow was never used and has been replaced by matrix strategy in ingest.yml
# Kept for reference only - DO NOT USE
# Archived: 2025-11-02
# Reason: Redundant - matrix strategy in ingest.yml provides same functionality with better parallelization

name: Reusable Ingestion Workflow (ARCHIVED - DO NOT USE)

on:
  workflow_call:
    inputs:
      script_name:
        required: true
        type: string
      source_name:
        required: true
        type: string

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.11
          cache: pip
      - run: pip install -r requirements.txt
      - run: python scripts/${{ inputs.script_name }}
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "data(${{ inputs.source_name }}): automated ingestion"

